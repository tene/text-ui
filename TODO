Block -> TextBlock

RenderBound
    min/max height/width

textblock.fit(bound)

ctx.render(widget)
ctx.render_bound(widget, bound)

migrate core into executor type




widget::list

widget::focusedlist

enum Segment {
    Text { length: usize, text: String }
    Border { length: usize, style: BorderStyle }
}